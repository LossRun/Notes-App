{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "Minimal offline notes application",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for vanilla JS'"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}